include $(GNUSTEP_MAKEFILES)/common.make

APP_NAME = CAActionPlayground

CAActionPlayground_OBJC_FILES = main.m
CAActionPlayground_OBJC_LIBS = -lGL -lQuartzCore -lopal

include $(GNUSTEP_MAKEFILES)/application.make